# Pin the web stack to a known-compatible pair. Starlette releases after 1.0
# changed GZipResponder's constructor and can crash Streamlit's ASGI server.
streamlit==1.60.0
starlette==1.0.0

pandas>=2.1,<3
numpy>=1.26,<3
requests>=2.31,<3
xgboost>=2.1,<4
scikit-learn>=1.4,<2
plotly>=5.22,<7
